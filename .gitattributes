# Git Attributes for Plated Project
# Ensures consistent line endings across Windows/Mac/Linux development environments

# ============================================================================
# AUTO DETECTION
# ============================================================================
# Auto detect text files and normalize line endings to LF on checkin
* text=auto

# ============================================================================
# SHELL SCRIPTS - CRITICAL: Must always use LF (Unix line endings)
# ============================================================================
# deploy.sh MUST have LF endings or it will fail on Linux production servers
*.sh text eol=lf