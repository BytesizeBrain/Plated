### Next:
1. Update post ("post_creation) so that users can upload an image, stores the image URL, save caption text, and user_id. 
--> DONE
2. Make sure that uploaded images don't over-write each other by adding a unique ID into file name.  
--> DONE
3. Connect uploaded images with a user post.
--> DONE
4. Create a /feed route that shows everyone's uploads (list all uploaded images)  
--> DONE


